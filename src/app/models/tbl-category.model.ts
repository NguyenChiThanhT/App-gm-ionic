export interface TblCategory {
  id: number | string;
  category_name: string;
  category_name_kana: string;
  status: number | boolean;
  created: string;
  category_note: string;
  sort_no: number;
  display: number | boolean;
}
